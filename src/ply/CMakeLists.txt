CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

OPTION(BUILD_TEST "Build test" ON)

ADD_LIBRARY(ply #SHARED
  plyfile.c
)

IF(BUILD_TEST)
  ADD_EXECUTABLE(plytest
    plytest.c  
  )
  TARGET_LINK_LIBRARIES(plytest
    ply
  )
ENDIF(BUILD_TEST)
